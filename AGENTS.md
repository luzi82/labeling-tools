# Development Instructions

Use the project's Python environment at `/opt/venv/bin/python` for all Python commands.

Examples:

```bash
/opt/venv/bin/python main.py
/opt/venv/bin/python -m pip install -r requirements.txt
```